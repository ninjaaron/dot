#!/usr/bin/env bash

# Run as root!

snapshot_number=$1
directory_path=$2
IFS="
"

files=$( snapper -c home status "${snapshot_number}..0" |
          grep "$directory_path" |
          cut -d' ' -f2-50 )


snapper -c home undochange "${snapshot_number}..0" $files

exit
